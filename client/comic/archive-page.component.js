import template from './archive-page.html';

import ArchivePageController from './archive-page.controller';

const archivePageComponent = {
    template,
    controller: ArchivePageController,
    controllerAs: 'archCtrl',
};

export default archivePageComponent;

